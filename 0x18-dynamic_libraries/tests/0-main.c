#include "../main.h"
#include <stdio.h>

/**
 * main - check the code for Alx students.
 *
 * Return: Always EXIT_SUCCESS.
 */
int main(void)
{
	printf("%d\n", _strlen("ALX SE"));
	return (EXIT_SUCCESS);
}
